@model List<Category>

<div class="list-group">

    <a asp-controller="Product" asp-action="List" asp-route-itemid=""
        class="list-group-item list-group-action @(ViewBag.SelectedCategory == null ? "active" : "")">Tüm
        Kategoriler</a>
    @foreach (var category in Model)
    {
        <a asp-controller="Product" asp-action="List" asp-route-itemid="@category.CategoryId"
            class="list-group-item list-group-action @(ViewBag.SelectedCategory != null && ViewBag.SelectedCategory.ToString() == category.CategoryId.ToString() ? "active" : "")">@category.Name</a>
    }
</div>
